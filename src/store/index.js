import { createStore } from 'vuex';
import modal from '@/store/modal/modal';

export default createStore({
    state: {
    },
    getters: {
    },
    mutations: {
    },
    actions: {
    },
    modules: {
        modal,
    },
});